# 依賴與建置產物已由根 .gitignore 涵蓋（node_modules、.next、.open-next、
# .wrangler、.env*、.dev.vars*、*.log、.DS_Store）；此處只列應用專屬條目。

# TypeScript 自動產生檔案
*.tsbuildinfo
next-env.d.ts

# 本機 UI 預覽用的 D1 種子檔
.preview-seed.sql
